* srand: 
    - is a random number generator function which will randomize the number produced by rand function.
    - The pseudo-random number generator
        is initialized using the argument passed as seed.

* rand() return the same result in each run

